import React, { useState, useEffect, useRef } from 'react';

const FrameFlowPro = () => {
  const [activeFeature, setActiveFeature] = useState(0);
  const [scrollProgress, setScrollProgress] = useState(0);
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [isLoading, setIsLoading] = useState(true);
  const [visibleSections, setVisibleSections] = useState(new Set());
  
  const sectionRefs = useRef({});
  const observerRef = useRef(null);

  const features = [
    {
      icon: "✨",
      title: "AI-Powered Editing",
      description: "Intelligent tools that learn your style and automate complex edits",
      color: "#0071e3",
      stats: "60% faster editing"
    },
    {
      icon: "⚡",
      title: "Real-Time 4K",
      description: "Butter-smooth 4K editing with instant preview and rendering",
      color: "#bf5af2",
      stats: "8x faster rendering"
    },
    {
      icon: "👥",
      title: "Team Collaboration",
      description: "Seamless real-time collaboration with version control",
      color: "#30d158",
      stats: "Unlimited collaborators"
    }
  ];

  const testimonials = [
    {
      name: "Sarah Chen",
      role: "Creative Director, Pixel Studio",
      content: "FrameFlow Pro has revolutionized our workflow. The AI tools cut our editing time by 60%.",
      avatar: "SC",
      rating: 5
    },
    {
      name: "Marcus Johnson",
      role: "Documentary Filmmaker",
      content: "The real-time collaboration features are game-changing for remote production teams.",
      avatar: "MJ",
      rating: 5
    },
    {
      name: "Alex Rodriguez",
      role: "Creative Lead, Studio Alpha",
      content: "The rendering speed is unbelievable. What used to take hours now happens in minutes.",
      avatar: "AR",
      rating: 5
    }
  ];

  const pricingPlans = [
    {
      name: "Starter",
      price: "$29",
      period: "per month",
      features: ["1080p Export", "10GB Storage", "Basic AI Tools", "1 Editor"],
      popular: false,
      color: "#0071e3"
    },
    {
      name: "Professional",
      price: "$79",
      period: "per month",
      features: ["4K Export", "100GB Storage", "Advanced AI", "3 Editors", "Team Collaboration"],
      popular: true,
      color: "#bf5af2"
    },
    {
      name: "Enterprise",
      price: "Custom",
      period: "tailored",
      features: ["8K Export", "Unlimited Storage", "All AI Features", "Unlimited Editors", "Priority Support"],
      popular: false,
      color: "#30d158"
    }
  ];

  // Initialize loading with enhanced animation
  useEffect(() => {
    const timer = setTimeout(() => {
      setIsLoading(false);
      document.body.style.overflow = 'auto';
    }, 2800);
    
    // Prevent scrolling during loading
    document.body.style.overflow = 'hidden';
    
    return () => {
      clearTimeout(timer);
      document.body.style.overflow = 'auto';
    };
  }, []);

  // Enhanced scroll progress with momentum
  useEffect(() => {
    let ticking = false;
    
    const handleScroll = () => {
      if (!ticking) {
        requestAnimationFrame(() => {
          const totalScroll = document.documentElement.scrollHeight - window.innerHeight;
          const progress = totalScroll > 0 ? (window.scrollY / totalScroll) * 100 : 0;
          setScrollProgress(progress);
          ticking = false;
        });
        ticking = true;
      }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  // Intersection Observer for scroll animations
  useEffect(() => {
    observerRef.current = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            setVisibleSections(prev => new Set(prev).add(entry.target.id));
          }
        });
      },
      {
        threshold: 0.15,
        rootMargin: '-50px 0px -50px 0px'
      }
    );

    // Observe all sections
    Object.values(sectionRefs.current).forEach(ref => {
      if (ref) observerRef.current.observe(ref);
    });

    return () => {
      if (observerRef.current) {
        observerRef.current.disconnect();
      }
    };
  }, []);

  // Auto-rotate features with enhanced timing
  useEffect(() => {
    const interval = setInterval(() => {
      setActiveFeature((prev) => (prev + 1) % features.length);
    }, 4500);
    return () => clearInterval(interval);
  }, [features.length]);

  const scrollToSection = (sectionId) => {
    const element = document.getElementById(sectionId);
    if (element) {
      const offset = 80; // Account for fixed header
      const elementPosition = element.getBoundingClientRect().top;
      const offsetPosition = elementPosition + window.pageYOffset - offset;

      window.scrollTo({
        top: offsetPosition,
        behavior: 'smooth'
      });
    }
    setIsMenuOpen(false);
  };

  // Enhanced loading animation with Apple-style polish
  if (isLoading) {
    return (
      <div className="fixed inset-0 bg-black flex flex-col justify-center items-center z-50 gap-8">
        <div className="relative">
          <div className="w-20 h-20 bg-gradient-to-br from-blue-500 to-purple-500 rounded-2xl flex items-center justify-center font-bold text-white text-lg animate-pulse shadow-2xl shadow-blue-500/30">
            FF
          </div>
          <div className="absolute inset-0 rounded-2xl border-2 border-blue-400/30 animate-ping"></div>
        </div>
        <div className="flex items-center gap-3 font-semibold text-2xl text-white">
          <span className="animate-fadeIn">FrameFlow</span>
          <sup className="text-sm bg-gradient-to-br from-blue-500 to-purple-500 bg-clip-text text-transparent animate-fadeIn" style={{animationDelay: '0.5s'}}>Pro</sup>
        </div>
        <div className="w-64 h-1 bg-gray-800 rounded-full overflow-hidden">
          <div className="h-full bg-gradient-to-r from-blue-500 via-purple-500 to-blue-500 animate-loadingBar"></div>
        </div>
      </div>
    );
  }

  const isSectionVisible = (sectionId) => visibleSections.has(sectionId);

  return (
    <div className="bg-black text-white font-sans overflow-x-hidden">
      {/* Enhanced Progress Bar */}
      <div 
        className="fixed top-0 left-0 h-1 bg-gradient-to-r from-blue-500 via-purple-500 to-blue-500 z-50 transition-all duration-300 ease-out shadow-2xl shadow-blue-500/30"
        style={{ 
          width: `${scrollProgress}%`,
          backgroundSize: '200% 100%',
          animation: 'gradientShift 2s ease infinite'
        }} 
      />

      {/* Enhanced Navigation */}
      <nav className={`fixed top-0 left-0 w-full z-40 backdrop-blur-xl border-b transition-all duration-500 ${
        scrollProgress > 5 ? 'bg-black/90 border-white/20' : 'bg-black/50 border-white/10'
      }`}>
        <div className="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
          <div className="flex items-center gap-3 font-semibold text-xl text-white">
            <div className="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-500 rounded-lg flex items-center justify-center font-bold text-xs shadow-lg">
              FF
            </div>
            <span className="bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">FrameFlow</span>
            <sup className="text-xs bg-gradient-to-br from-blue-500 to-purple-500 bg-clip-text text-transparent ml-0.5">Pro</sup>
          </div>
          
          <div className="hidden md:flex gap-10">
            {['features', 'pricing', 'showcase', 'demo'].map((item) => (
              <button 
                key={item}
                className="text-gray-400 text-sm font-medium transition-all duration-300 relative py-2 hover:text-white group"
                onClick={() => scrollToSection(item)}
              >
                {item.charAt(0).toUpperCase() + item.slice(1)}
                <span className="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-blue-500 to-purple-500 transition-all duration-500 group-hover:w-full"></span>
              </button>
            ))}
          </div>

          <div className="hidden md:flex gap-3">
            <button className="px-5 py-2.5 rounded-2xl text-sm font-semibold transition-all duration-300 flex items-center gap-2 border border-gray-600 text-gray-300 hover:border-gray-400 hover:text-white hover:-translate-y-0.5">
              Sign In
            </button>
            <button className="px-5 py-2.5 rounded-2xl text-sm font-semibold transition-all duration-300 flex items-center gap-2 bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:shadow-2xl hover:shadow-blue-500/25 hover:-translate-y-0.5">
              Try Free →
            </button>
          </div>

          <button 
            className="md:hidden p-2.5 rounded-lg bg-white/5 hover:bg-white/10 transition-colors duration-300"
            onClick={() => setIsMenuOpen(!isMenuOpen)}
            aria-label="Toggle menu"
          >
            <div className="w-5 h-3.5 relative">
              <span className={`absolute left-0 w-full h-0.5 bg-white transition-all duration-300 ${
                isMenuOpen ? 'rotate-45 top-1/2 -translate-y-1/2' : 'top-0'
              }`}></span>
              <span className={`absolute left-0 w-full h-0.5 bg-white transition-all duration-300 ${
                isMenuOpen ? '-rotate-45 top-1/2 -translate-y-1/2' : 'bottom-0'
              }`}></span>
            </div>
          </button>
        </div>
      </nav>

      {/* Enhanced Mobile Menu */}
      <div className={`fixed top-16 left-0 w-full bg-black/95 backdrop-blur-2xl border-b border-white/10 transition-all duration-500 z-30 md:hidden ${
        isMenuOpen ? 'translate-y-0 opacity-100' : '-translate-y-4 opacity-0 pointer-events-none'
      }`}>
        <div className="p-6 flex flex-col gap-1">
          {['features', 'pricing', 'showcase', 'demo', 'sign-in'].map((item) => (
            <button 
              key={item}
              className="text-gray-300 text-base font-medium text-left py-4 px-4 rounded-xl transition-all duration-300 hover:bg-white/5 hover:text-white hover:pl-6"
              onClick={() => scrollToSection(item)}
            >
              {item.split('-').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ')}
            </button>
          ))}
        </div>
      </div>

      {/* Enhanced Hero Section */}
      <section 
        ref={el => sectionRefs.current.hero = el}
        id="hero" 
        className="min-h-screen relative overflow-hidden bg-gradient-to-br from-black via-gray-900 to-black flex items-center"
      >
        {/* Animated Background Elements */}
        <div className="absolute inset-0 overflow-hidden">
          <div className="absolute w-80 h-80 bg-gradient-to-br from-blue-500/20 to-purple-500/20 rounded-full blur-3xl top-1/4 -left-20 animate-float"></div>
          <div className="absolute w-60 h-60 bg-gradient-to-br from-purple-500/15 to-pink-500/15 rounded-full blur-3xl top-1/2 -right-20 animate-float" style={{animationDelay: '2s'}}></div>
          <div className="absolute w-40 h-40 bg-gradient-to-br from-blue-500/10 to-cyan-500/10 rounded-full blur-3xl bottom-1/4 left-1/4 animate-float" style={{animationDelay: '4s'}}></div>
        </div>

        <div className="max-w-7xl mx-auto px-6 grid grid-cols-1 lg:grid-cols-2 items-center gap-20 relative z-10">
          <div className="pt-20 space-y-8">
            <div className="inline-flex items-center gap-3 bg-white/10 backdrop-blur-xl px-5 py-3 rounded-2xl text-sm font-semibold border border-white/10 animate-pulse">
              <span className="text-lg animate-spin" style={{animationDuration: '3s'}}>✨</span>
              <span>Now with AI Assistant</span>
            </div>

            <div className="space-y-6">
              <h1 className="text-5xl md:text-6xl lg:text-7xl font-bold leading-tight">
                <span className={`block bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent transition-all duration-1000 ${
                  isSectionVisible('hero') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}>Professional Video</span>
                <span className={`block bg-gradient-to-br from-blue-400 via-purple-500 to-cyan-400 bg-clip-text text-transparent transition-all duration-1000 delay-300 ${
                  isSectionVisible('hero') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}>Editing Evolved</span>
              </h1>

              <p className={`text-xl text-gray-300 max-w-lg leading-relaxed transition-all duration-1000 delay-500 ${
                isSectionVisible('hero') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
              }`}>
                The world's most advanced cloud-native video editing platform. 
                Powered by AI, designed for creators, built for teams.
              </p>
            </div>

            <div className={`flex gap-4 flex-wrap transition-all duration-1000 delay-700 ${
              isSectionVisible('hero') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
            }`}>
              <button className="group px-8 py-4 rounded-2xl text-base font-semibold transition-all duration-500 flex items-center gap-3 bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:shadow-2xl hover:shadow-blue-500/30 hover:-translate-y-1 relative overflow-hidden">
                <span className="group-hover:scale-110 transition-transform duration-300">▶</span>
                Start Free Trial
                <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
              </button>
              <button className="group px-8 py-4 rounded-2xl text-base font-semibold transition-all duration-500 flex items-center gap-3 border border-gray-600 text-gray-300 hover:border-gray-400 hover:text-white hover:-translate-y-1 relative overflow-hidden">
                <span>📽️</span>
                Watch Demo
                <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
              </button>
            </div>

            <div className={`flex gap-12 transition-all duration-1000 delay-900 ${
              isSectionVisible('hero') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
            }`}>
              {[
                { number: "4K+", label: "Professional Creators" },
                { number: "98%", label: "Faster Rendering" },
                { number: "24/7", label: "Real-time Support" }
              ].map((stat, index) => (
                <div key={index} className="text-left">
                  <div className="text-2xl font-bold mb-2 bg-gradient-to-br from-blue-400 to-purple-400 bg-clip-text text-transparent">
                    {stat.number}
                  </div>
                  <div className="text-sm text-gray-400 font-medium">{stat.label}</div>
                </div>
              ))}
            </div>
          </div>

          <div className={`flex justify-center items-center transition-all duration-1000 delay-300 ${
            isSectionVisible('hero') ? 'opacity-100 translate-x-0' : 'opacity-0 translate-x-12'
          }`}>
            <div className="relative group">
              <div className="w-full max-w-md h-72 bg-gray-800 rounded-2xl relative shadow-2xl border border-white/10 transform perspective-1000 rotate-y-[-5deg] rotate-x-5 transition-all duration-700 group-hover:rotate-y-0 group-hover:rotate-x-0 group-hover:scale-105">
                <div className="w-full h-full bg-black rounded-xl overflow-hidden">
                  <div className="p-6 h-full flex flex-col gap-6">
                    <div className="flex-1 bg-gray-800 rounded-xl p-6 relative overflow-hidden">
                      {[0, 1, 2].map((i) => (
                        <div key={i} className="h-2 bg-gray-700 rounded mb-4 relative overflow-hidden">
                          <div 
                            className="absolute inset-0 bg-gradient-to-r from-blue-500 to-purple-500 animate-pulse"
                            style={{animationDuration: '3s', animationDelay: `${i * 0.5}s`}}
                          ></div>
                        </div>
                      ))}
                      <div className="absolute top-0 left-1/3 w-0.5 h-full bg-blue-500 shadow-lg shadow-blue-500 animate-pulse" style={{animationDuration: '3s'}}></div>
                    </div>
                    <div className="h-16 bg-gray-800 rounded-xl flex items-center gap-6 px-6">
                      {[0, 1, 2].map(i => (
                        <div key={i} className="w-20 h-2 bg-gray-700 rounded animate-pulse" style={{animationDelay: `${i * 0.2}s`}}></div>
                      ))}
                    </div>
                  </div>
                </div>
                <div className="absolute -bottom-6 -left-8 -right-8 h-6 bg-gradient-to-t from-gray-900 to-transparent rounded-b-2xl"></div>
              </div>
              <div className="absolute -inset-4 bg-gradient-to-r from-blue-500/20 to-purple-500/20 rounded-3xl blur-xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 -z-10"></div>
            </div>
          </div>
        </div>

        {/* Enhanced Scroll Indicator */}
        <div className={`absolute bottom-8 left-1/2 transform -translate-x-1/2 flex flex-col items-center gap-3 text-gray-400 text-sm z-10 transition-all duration-1000 delay-1000 ${
          isSectionVisible('hero') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
        }`}>
          <div className="w-0.5 h-12 bg-gradient-to-b from-blue-500 to-purple-500 rounded-full animate-bounce">
            <div className="w-2 h-2 bg-gradient-to-br from-blue-400 to-purple-400 rounded-full mx-auto -mt-1 animate-pulse"></div>
          </div>
          <span className="font-medium">Scroll to explore</span>
        </div>
      </section>

      {/* Enhanced Features Section */}
      <section 
        ref={el => sectionRefs.current.features = el}
        id="features" 
        className="py-24 bg-black relative overflow-hidden"
      >
        <div className="max-w-7xl mx-auto px-6">
          <div className={`text-center mb-20 transition-all duration-1000 ${
            isSectionVisible('features') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
          }`}>
            <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">
              Why Creators Choose FrameFlow
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto font-light">
              Built with cutting-edge technology for unparalleled performance
            </p>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
            <div className="flex flex-col gap-6">
              {features.map((feature, index) => (
                <div
                  key={index}
                  className={`p-8 rounded-3xl bg-gradient-to-br from-white/5 to-white/2 border border-white/10 cursor-pointer transition-all duration-500 flex gap-6 items-start relative overflow-hidden group hover:from-white/8 hover:to-white/5 hover:border-white/20 hover:scale-105 hover:shadow-2xl ${
                    activeFeature === index ? 'from-white/10 to-white/5 border-white/20 scale-105 shadow-xl' : ''
                  } ${
                    isSectionVisible('features') ? 'opacity-100 translate-x-0' : 'opacity-0 -translate-x-8'
                  }`}
                  style={{transitionDelay: `${index * 150}ms`}}
                  onMouseEnter={() => setActiveFeature(index)}
                >
                  <div 
                    className="w-14 h-14 flex items-center justify-center bg-white/10 rounded-2xl text-2xl transition-all duration-500 group-hover:scale-110 group-hover:shadow-lg flex-shrink-0"
                    style={{ color: feature.color }}
                  >
                    {feature.icon}
                  </div>
                  <div className="flex-1 space-y-3">
                    <h3 className="text-2xl font-semibold text-white">{feature.title}</h3>
                    <p className="text-gray-300 leading-relaxed font-light">{feature.description}</p>
                    <div className="text-blue-400 font-semibold text-sm">{feature.stats}</div>
                  </div>
                  <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
                </div>
              ))}
            </div>

            <div className={`relative h-[600px] transition-all duration-1000 delay-300 ${
              isSectionVisible('features') ? 'opacity-100 translate-x-0' : 'opacity-0 translate-x-8'
            }`}>
              <div className="relative w-full h-full">
                {/* Floating Cards */}
                <div className="absolute top-20 left-4 px-5 py-4 bg-white/10 backdrop-blur-xl border border-white/10 rounded-2xl flex items-center gap-3 text-sm font-semibold animate-float shadow-xl">
                  <span className="text-lg">🎬</span>
                  <span>4K Export</span>
                </div>
                <div className="absolute top-1/2 right-4 px-5 py-4 bg-white/10 backdrop-blur-xl border border-white/10 rounded-2xl flex items-center gap-3 text-sm font-semibold animate-float shadow-xl" style={{animationDelay: '2s'}}>
                  <span className="text-lg">✏️</span>
                  <span>AI Tools</span>
                </div>
                <div className="absolute bottom-32 left-20 px-5 py-4 bg-white/10 backdrop-blur-xl border border-white/10 rounded-2xl flex items-center gap-3 text-sm font-semibold animate-float shadow-xl" style={{animationDelay: '4s'}}>
                  <span className="text-lg">📥</span>
                  <span>Cloud Sync</span>
                </div>
                
                {/* Main Visual */}
                <div className="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-80 h-52 bg-gray-800 rounded-3xl border border-white/10 overflow-hidden shadow-2xl group hover:scale-105 transition-transform duration-500">
                  <div className="p-6 h-full flex flex-col gap-4">
                    <div className="flex-1 bg-gradient-to-r from-transparent via-blue-500/20 to-transparent rounded-lg animate-pulse"></div>
                    <div className="h-5 bg-gradient-to-r from-red-500 via-orange-500 via-green-500 via-blue-500 to-purple-500 rounded-full animate-hue-rotate"></div>
                    <div className="h-5 bg-gray-700 rounded-full relative overflow-hidden">
                      <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent animate-shimmer"></div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Enhanced Demo Section */}
      <section 
        ref={el => sectionRefs.current.demo = el}
        id="demo" 
        className="py-24 bg-gradient-to-br from-gray-900 via-black to-gray-900 relative overflow-hidden"
      >
        <div className="absolute inset-0 bg-gradient-to-r from-blue-500/5 via-transparent to-purple-500/5"></div>
        <div className="max-w-7xl mx-auto px-6 relative z-10">
          <div className={`text-center mb-20 transition-all duration-1000 ${
            isSectionVisible('demo') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
          }`}>
            <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">
              Experience Advanced Video Editing
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto font-light">
              Interactive timeline and real-time preview with advanced features
            </p>
          </div>
          
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
            <div className={`bg-white/5 backdrop-blur-xl border border-white/10 rounded-3xl p-8 transition-all duration-1000 ${
              isSectionVisible('demo') ? 'opacity-100 translate-x-0' : 'opacity-0 -translate-x-8'
            }`}>
              <h3 className="text-2xl font-semibold mb-4 bg-gradient-to-br from-blue-400 to-purple-400 bg-clip-text text-transparent">
                Advanced Timeline
              </h3>
              <p className="text-gray-300 mb-8 leading-relaxed font-light">
                Experience the future of video editing with our advanced timeline features and real-time preview
              </p>
              <div className="space-y-4">
                {['Play Preview', 'Export Project', 'AI Enhance'].map((text, index) => (
                  <button 
                    key={text}
                    className="w-full px-6 py-4 bg-white/10 border border-white/10 rounded-2xl font-semibold transition-all duration-300 text-center hover:bg-white/20 hover:-translate-y-1 hover:border-blue-500 hover:shadow-lg"
                    style={{transitionDelay: `${index * 100}ms`}}
                  >
                    {text}
                  </button>
                ))}
              </div>
            </div>
            
            <div className={`lg:col-span-2 w-full h-96 bg-gradient-to-br from-gray-900 to-black rounded-3xl overflow-hidden border border-white/10 relative flex items-center justify-center group transition-all duration-1000 ${
              isSectionVisible('demo') ? 'opacity-100 translate-x-0' : 'opacity-0 translate-x-8'
            }`}>
              <div className="text-center space-y-6">
                <div className="w-24 h-24 bg-gradient-to-br from-blue-500 to-purple-500 rounded-3xl mx-auto flex items-center justify-center text-white text-2xl font-bold shadow-2xl animate-pulse">
                  FF
                </div>
                <div className="space-y-2">
                  <h3 className="text-3xl font-bold text-white">Advanced Editor</h3>
                  <p className="text-gray-400 font-light">Experience the power of FrameFlow Pro</p>
                </div>
              </div>
              <div className="absolute inset-0 bg-gradient-to-r from-blue-500/10 to-purple-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            </div>
          </div>
        </div>
      </section>

      {/* Enhanced Pricing Section */}
      <section 
        ref={el => sectionRefs.current.pricing = el}
        id="pricing" 
        className="py-24 bg-gradient-to-br from-black via-gray-900 to-black"
      >
        <div className="max-w-7xl mx-auto px-6">
          <div className={`text-center mb-20 transition-all duration-1000 ${
            isSectionVisible('pricing') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
          }`}>
            <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">
              Simple, Transparent Pricing
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto font-light">
              Start free and scale as you grow
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {pricingPlans.map((plan, index) => (
              <div 
                key={index}
                className={`p-8 rounded-3xl bg-gradient-to-br from-white/5 to-white/2 border transition-all duration-500 relative text-center group hover:scale-105 hover:shadow-2xl ${
                  plan.popular 
                    ? 'from-blue-500/20 to-purple-500/20 border-blue-500/30 scale-105 shadow-xl' 
                    : 'border-white/10 hover:border-white/20'
                } ${
                  isSectionVisible('pricing') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}
                style={{transitionDelay: `${index * 200}ms`}}
              >
                {plan.popular && (
                  <div className="absolute -top-3 left-1/2 transform -translate-x-1/2 bg-gradient-to-r from-blue-600 to-purple-600 text-white px-6 py-1.5 rounded-full text-xs font-semibold shadow-lg">
                    Most Popular
                  </div>
                )}
                <div className="mb-8">
                  <h3 className="text-2xl font-semibold mb-4 text-white">{plan.name}</h3>
                  <div className="flex items-baseline justify-center gap-2">
                    <span className="text-5xl font-bold bg-gradient-to-br from-blue-400 to-purple-400 bg-clip-text text-transparent">
                      {plan.price}
                    </span>
                    <span className="text-gray-400 font-light">/{plan.period}</span>
                  </div>
                </div>
                <ul className="mb-8 text-left space-y-3">
                  {plan.features.map((feature, idx) => (
                    <li key={idx} className="flex items-center gap-3 text-gray-300 font-light">
                      <span className="text-green-400 font-bold text-lg">✓</span>
                      {feature}
                    </li>
                  ))}
                </ul>
                <button 
                  className="w-full px-6 py-4 rounded-2xl font-semibold transition-all duration-300 text-white hover:-translate-y-1 hover:shadow-lg group relative overflow-hidden"
                  style={{ background: plan.color }}
                >
                  Get Started
                  <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-700"></div>
                </button>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Enhanced Showcase Section */}
      <section 
        ref={el => sectionRefs.current.showcase = el}
        id="showcase" 
        className="py-24 bg-black"
      >
        <div className="max-w-7xl mx-auto px-6">
          <div className={`text-center mb-20 transition-all duration-1000 ${
            isSectionVisible('showcase') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
          }`}>
            <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">
              Loved by Industry Leaders
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto font-light">
              Trusted by the world's most innovative teams
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {testimonials.map((testimonial, index) => (
              <div 
                key={index}
                className={`p-8 rounded-3xl bg-gradient-to-br from-white/5 to-white/2 border border-white/10 transition-all duration-500 relative overflow-hidden group hover:scale-105 hover:border-white/20 hover:shadow-xl ${
                  isSectionVisible('showcase') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}
                style={{transitionDelay: `${index * 150}ms`}}
              >
                <div className="flex gap-1 mb-6">
                  {[...Array(testimonial.rating)].map((_, i) => (
                    <span key={i} className="text-yellow-400 text-lg">⭐</span>
                  ))}
                </div>
                <div className="text-lg leading-relaxed mb-8 text-gray-200 font-light italic">
                  <p>"{testimonial.content}"</p>
                </div>
                <div className="flex items-center gap-4">
                  <div className="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-2xl flex items-center justify-center font-semibold text-sm text-white shadow-lg">
                    {testimonial.avatar}
                  </div>
                  <div>
                    <div className="font-semibold text-white">{testimonial.name}</div>
                    <div className="text-sm text-gray-400 font-light">{testimonial.role}</div>
                  </div>
                </div>
                <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Enhanced CTA Section */}
      <section 
        ref={el => sectionRefs.current.cta = el}
        id="cta" 
        className="py-24 bg-gradient-to-br from-black via-gray-900 to-black text-center relative overflow-hidden"
      >
        <div className="absolute inset-0">
          <div className="absolute w-96 h-96 bg-blue-500/10 rounded-full blur-3xl -top-48 -left-48 animate-pulse"></div>
          <div className="absolute w-96 h-96 bg-purple-500/10 rounded-full blur-3xl -bottom-48 -right-48 animate-pulse" style={{animationDelay: '2s'}}></div>
        </div>
        
        <div className="max-w-4xl mx-auto px-6 relative z-10">
          <div className={`space-y-8 transition-all duration-1000 ${
            isSectionVisible('cta') ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
          }`}>
            <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">
              Ready to Create Magic?
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto font-light">
              Start your free 14-day trial. No credit card required.
            </p>
            
            <div className="flex gap-6 justify-center mb-12 flex-wrap">
              <button className="group px-10 py-5 rounded-2xl text-lg font-semibold transition-all duration-500 bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:shadow-2xl hover:shadow-blue-500/30 hover:-translate-y-1 relative overflow-hidden">
                Get Started Free
                <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
              </button>
              <button className="group px-10 py-5 rounded-2xl text-lg font-semibold transition-all duration-500 border border-gray-600 text-gray-300 hover:border-gray-400 hover:text-white hover:-translate-y-1 relative overflow-hidden">
                Schedule Demo
                <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent -skew-x-12 translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
              </button>
            </div>
            
            <div className="flex gap-8 justify-center flex-wrap text-gray-400 font-light">
              {['Free 14-day trial', 'No credit card required', 'Cancel anytime'].map((text, index) => (
                <div key={text} className="flex items-center gap-2 text-sm">
                  <span className="text-green-400">✓</span>
                  {text}
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* Enhanced Footer */}
      <footer className="py-16 bg-black border-t border-white/10">
        <div className="max-w-7xl mx-auto px-6">
          <div className="grid grid-cols-1 lg:grid-cols-4 gap-12 mb-12">
            <div className="lg:col-span-1 space-y-4">
              <div className="flex items-center gap-3 font-semibold text-xl text-white">
                <div className="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-500 rounded-lg flex items-center justify-center font-bold text-xs">
                  FF
                </div>
                <span className="bg-gradient-to-br from-white to-gray-300 bg-clip-text text-transparent">FrameFlow</span>
                <sup className="text-xs bg-gradient-to-br from-blue-500 to-purple-500 bg-clip-text text-transparent ml-0.5">Pro</sup>
              </div>
              <p className="text-gray-400 leading-relaxed font-light">
                Professional video editing for the modern creator.
              </p>
            </div>

            <div className="lg:col-span-3 grid grid-cols-1 md:grid-cols-3 gap-8">
              {[
                {
                  title: "Product",
                  links: ["Features", "Pricing", "Advanced Editor", "Updates"]
                },
                {
                  title: "Resources", 
                  links: ["Documentation", "Tutorials", "Community", "Support"]
                },
                {
                  title: "Company",
                  links: ["About", "Careers", "Contact", "Press"]
                }
              ].map((column, colIndex) => (
                <div key={column.title} className="space-y-4">
                  <h4 className="text-base font-semibold text-white">{column.title}</h4>
                  <div className="space-y-2">
                    {column.links.map((link, linkIndex) => (
                      <button 
                        key={link}
                        className="block text-gray-400 text-sm font-light transition-all duration-300 hover:text-white hover:pl-2 text-left w-full"
                        onClick={() => link === 'Features' ? scrollToSection('features') : 
                                 link === 'Pricing' ? scrollToSection('pricing') :
                                 link === 'Advanced Editor' ? scrollToSection('demo') : null}
                      >
                        {link}
                      </button>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div className="flex flex-col md:flex-row justify-between items-center pt-8 border-t border-white/10 text-gray-400 text-sm">
            <div className="mb-4 md:mb-0 font-light">
              © 2024 FrameFlow Pro. All rights reserved.
            </div>
            <div className="flex gap-6">
              {['Privacy', 'Terms', 'Cookies'].map((item) => (
                <button key={item} className="text-gray-400 transition-colors duration-300 hover:text-white font-light">
                  {item}
                </button>
              ))}
            </div>
          </div>
        </div>
      </footer>

      {/* Enhanced Back to Top */}
      <button 
        className={`fixed bottom-8 right-8 w-14 h-14 bg-gradient-to-br from-blue-600 to-purple-600 text-white rounded-2xl flex items-center justify-center text-xl transition-all duration-500 z-40 shadow-2xl hover:-translate-y-1 hover:shadow-3xl backdrop-blur-sm ${
          scrollProgress > 10 ? 'opacity-100 scale-100' : 'opacity-0 scale-50 pointer-events-none'
        }`}
        onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}
      >
        ↑
      </button>

      {/* Enhanced Animations */}
      <style jsx>{`
        @keyframes fadeInUp {
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
        @keyframes fadeInRight {
          to {
            opacity: 1;
            transform: translateX(0);
          }
        }
        @keyframes float {
          0%, 100% { transform: translateY(0px) rotate(0deg); }
          50% { transform: translateY(-20px) rotate(5deg); }
        }
        @keyframes gradientShift {
          0%, 100% { background-position: 0% 50%; }
          50% { background-position: 100% 50%; }
        }
        @keyframes loadingBar {
          0% { transform: translateX(-100%); }
          50% { transform: translateX(0%); }
          100% { transform: translateX(100%); }
        }
        @keyframes hue-rotate {
          0% { filter: hue-rotate(0deg); }
          100% { filter: hue-rotate(360deg); }
        }
        @keyframes shimmer {
          0% { transform: translateX(-100%); }
          100% { transform: translateX(100%); }
        }
        
        .animate-fadeIn {
          animation: fadeInUp 0.8s ease-out forwards;
        }
        .animate-float {
          animation: float 6s ease-in-out infinite;
        }
        .animate-loadingBar {
          animation: loadingBar 2s ease-in-out infinite;
        }
        .animate-hue-rotate {
          animation: hue-rotate 3s linear infinite;
        }
        .animate-shimmer {
          animation: shimmer 2s ease-in-out infinite;
        }
        
        .perspective-1000 {
          perspective: 1000px;
        }
        
        .rotate-y-5 {
          transform: rotateY(5deg);
        }
        
        .rotate-y-[-5deg] {
          transform: rotateY(-5deg);
        }
        
        .rotate-x-5 {
          transform: rotateX(5deg);
        }
      `}</style>
    </div>
  );
};

export default FrameFlowPro;